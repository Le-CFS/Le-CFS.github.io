---
label: SEO
hide_body: true
fields:
- name: type
  type: select
  default: article
  config:
    required: true
    options:
    - article
    - video
    source:
      type: simple
      section: 
      file: 
      path: 
  label: Type
- name: description
  type: textarea
  default: ''
  config:
    required: false
    wysiwyg: false
    schema:
      format: markdown
  label: Description pour les moteurs de recherche
- name: img
  type: file
  config:
    maxSize: 64
  label: Image pour les moteurs de recherche
