---
label: SEO
hide_body: true
fields:
- name: description
  type: textarea
  default: ''
  config:
    required: false
    wysiwyg: false
    schema:
      format: markdown
  label: Description pour les moteurs de recherche
- name: image
  type: file
  config:
    maxSize: 64
  label: Image pour les moteurs de recherche
